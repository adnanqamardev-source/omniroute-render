{
  "name": "omniroute-render",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "npx omniroute serve --port $PORT --host 0.0.0.0"
  },
  "dependencies": {
    "omniroute": "latest"
  }
}
