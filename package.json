{
  "name": "omniroute-render",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "npx omniroute serve --port $PORT --no-open --log --no-tray"
  },
  "dependencies": {
    "omniroute": "^3.8.50"
  }
}
